# Klaviyo (public/site key only — never put a private key here)
# Account → Settings → API keys → Public API Key / Site ID
VITE_KLAVIYO_PUBLIC_KEY=

# List that homepage signups should join (Audience → Lists → list → Settings → List ID)
VITE_KLAVIYO_LIST_ID=
